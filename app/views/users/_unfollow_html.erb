<%= form_for(current_user.relationships.find_by_followed_id( @user),
            html: { method: :delete }) do |f| %>
 <div> <%= f.hidden_field :followed_id %> </div>
 <%= f.button "Unfollow", class: "btn btn-primary pull-right" %>
<% end -%>
