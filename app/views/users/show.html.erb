<% provide(:title, @user.name) %>

<div class="row">
  <div class="col-md-6">
      <%= image_tag @user.avatar.url(:medium), class: "img-rounded" %>
  </div>
  <div class="col-md-4 ">
    <h3><%= @user.name %></h3>
    <p><%=  @user.occupation%></p>
 </div>
</div>