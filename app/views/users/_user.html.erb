
   <div class="col-md-3">
       <div class="thumbnail"> 
       <%= link_to(image_tag(user.avatar.url(:medium)), user) %>
          <div class="caption">
             <h3><%= link_to user.name, user%></h3>
             <p><%= user.occupation %></p>
          </div> 
        </div>
      </div>
