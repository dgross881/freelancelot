 <div class="img-thumbnail">
<%= image_tag(current_user.avatar.url(:medium)) %>  
   <p><%= current_user.name %></p>
   <span ><%= link_to "view my profile", current_user %></span> 
   <span><%= pluralize(current_user.microposts.count, "messages") %></span> 
 </div>
</div>